import { internalConfig } from "@arcgis/storybook-utils/config";
import type { StorybookConfig } from "@storybook/web-components-vite";

const config: StorybookConfig = {
  ...internalConfig,
};
export default config;
