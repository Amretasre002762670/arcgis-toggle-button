import { externalConfig } from "@arcgis/storybook-utils/config";
import type { StorybookConfig } from "@storybook/web-components-vite";

const config: StorybookConfig = {
  ...externalConfig,
};
export default config;
